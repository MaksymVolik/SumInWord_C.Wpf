<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:CC="clr-namespace:SumInWord_C.Wpf.CustomControls">

    <Style TargetType="{x:Type CC:PlaceHolderTextBox}">
        <Setter Property="Background" Value="{DynamicResource BackgroundBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
        <Setter Property="CaretBrush" Value="{DynamicResource PrimaryTextBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource SecondaryBackgroundBrush}" />
        <Setter Property="BorderThickness" Value="2" />
        <Setter Property="Cursor" Value="IBeam" />
        <Setter Property="Padding" Value="5" />
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="FontSize" Value="20" />
        <Setter Property="HorizontalContentAlignment" Value="Right" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="PlaceHolder" Value="Placeholder?" />
        <Setter Property="PlaceholderFontSize" Value="14" />
        <Setter Property="PlaceholderHorizontalAlignment" Value="Center" />
        <Setter Property="PlaceholderVerticalAlignment" Value="Center" />

        <Setter Property="Validation.ErrorTemplate">
            <Setter.Value>
                <ControlTemplate/>
            </Setter.Value>
        </Setter>
        
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type CC:PlaceHolderTextBox}">
                    <Border
                        x:Name="_Border"
                        MinWidth="{TemplateBinding MinWidth}"
                        MinHeight="{TemplateBinding MinHeight}"
                        MaxWidth="{TemplateBinding MaxWidth}"
                        MaxHeight="{TemplateBinding MaxHeight}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{TemplateBinding CornerRadius}"
                        SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                        <Grid Margin="{TemplateBinding Padding}" Background="Transparent">
                            <ScrollViewer x:Name="PART_ContentHost" Padding="{TemplateBinding Padding}" />
                            <TextBlock
                                Name="_TextBlock"
                                Padding="{TemplateBinding Padding}"
                                HorizontalAlignment="{TemplateBinding PlaceholderHorizontalAlignment}"
                                VerticalAlignment="{TemplateBinding PlaceholderVerticalAlignment}"
                                Background="Transparent"
                                FontFamily="{TemplateBinding FontFamily}"
                                FontSize="{TemplateBinding PlaceholderFontSize}"
                                FontWeight="{TemplateBinding FontWeight}"
                                Foreground="{DynamicResource SecondaryTextBrush}"
                                Text="{TemplateBinding PlaceHolder}"
                                Visibility="Collapsed" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <!--  || When Text Block is empty, Show PlaceHolder ||  -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="Text" Value="" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="_TextBlock" Property="Visibility" Value="Visible" />
                        </MultiTrigger>
                        <!--  || Hover Effect ||  -->
                        <!--  || Focus Effect ||  -->
                        <Trigger Property="IsFocused" Value="False">
                            <Setter TargetName="_Border" Property="BorderBrush" Value="{DynamicResource SecondaryBackgroundBrush}"/>
                            <Setter TargetName="_Border" Property="BorderThickness" Value="1"/>
                            <Setter TargetName="_Border" Property="Margin" Value="1"/>
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="_Border" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            <Setter TargetName="_Border" Property="BorderThickness" Value="2"/>
                            <Setter TargetName="_Border" Property="Margin" Value="0"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="_Border" Property="BorderBrush" Value="{DynamicResource AccentHoverBrush}" />
                        </Trigger>
                        <Trigger Property="Validation.HasError" Value="True">
                            <Setter Property="ToolTip" 
                                    Value="{Binding RelativeSource={x:Static RelativeSource.Self}, 
                                            Path=(Validation.Errors)[0].ErrorContent}"/>
                            <Setter TargetName="_Border" Property="BorderBrush" Value="{DynamicResource ErrorBrush}"/>
                            <Setter TargetName="_Border" Property="BorderThickness" Value="2"/>
                            <Setter TargetName="_Border" Property="Margin" Value="0"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>